# Step 1: Clone your repository (only once per Colab session)
!git clone https://github.com/Pradipwasre/Python-Day-to-day.git

# Step 2: Change directory into the repo
%cd Python-Day-to-day

# Step 3: (Optional) Check files
!ls

# Step 4: To update with your latest commits later, run:
!git pull origin main
